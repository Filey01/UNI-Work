w_0_init = (1.625) # Initial weight of the aircraft
A = 0.86 # A set constant
C = -0.05 # A set constant
wp= (0.125) # Payload weight

# Raymer's equation, iterated i times
for i in range(100):
  w_0_init = wp / (1 - (A * w_0 ** C))
    
    # Return final weight in kg
  print(f"Iteration {i+1}: w0 = {w0}")
